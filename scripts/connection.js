/*function connectingDB(){
    var pg = require('pg');
    var connectionString = "postgres://userName:postgres@local/5432/PostgreSQL 14";
    var pgClient = new pg.Client(connectionString);
    pgClient.connect();
    var query = pgClient.query(`SELECT login_name,login_password from "Training".login where login_name =${varid}`);
}
function testing()
{

    alert("hi I am testing");
}*/

a[3];
console.log(a.len())
